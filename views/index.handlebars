<div id="allInfo" class="container">

<div id="firstSpace">

</div>

{{#each articles}}

<div class="media" id="eachC"id={{this._id}} >                                <!-- PARA MARCAR CADA CONTENEDOR CON EL ID DE LA NOTICIA -->
  <div class="media-body">
      <a href="{{this.link}}" target="_blank">{{this.title}}</a>    <!-- ENCABEZADO LINK PARA SEGUIR-->
    <h5 class="mt-0 mb-1">{{this.summary}}</h5>                     <!-- RESUMEN-->
    
    
  </div>

  <img class="ml-3" src="{{this.img}}" alt="NO IMAGE">              <!-- IMAGEN-->
</div>

{{/each}}

<!-- Write your comments here 


    {{#each articles}}
    <div class="container" id={{this._id}}>
        <h2>
            <a href="{{this.link}}" target="_blank">{{this.title}}</a>
            <form action="/save/{{this._id}}" method="POST">
                <button type="submit" class="status" value="{{this.status}}">{{this.status}}</button>
                {{#unless this.note}}
                <button type="button" class="addnote-button" value="{{this._id}}">Add A Note</button>
                {{/unless}}
                {{#if this.note}}
                <button type="button" class="addnote-button" value="{{this._id}}">Edit Note</button>
                {{/if}}
            </form>
        </h2>
        <summary>{{this.summary}}</summary>
        <img src="{{this.img}}">
    </div>
    {{/each}}

-->

</div>